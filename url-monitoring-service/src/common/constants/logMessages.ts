export enum ErrorLog {
  EMAIL_FAILURE = 'Could not send an email',
  MAIL_SENDER_FAILURE = 'Mail Sender Failed to send an email'
}

export enum Log {
  EMAIL_SENT_SUCCESS = 'Email sent successfully',
}